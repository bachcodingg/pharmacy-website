================================================================================
PHARMACY SEARCH - HANDOFF INSTRUCTIONS FOR CLAUDE
================================================================================
Written 2026-09-11, at the end of a session that audited this project against
Pharmacy-Search-Audit.pdf (15 findings, F-01..F-15), implemented most of those
fixes, and found 12 more issues by reading the code (N-01..N-12).

UPDATED 2026-09-18. Tasks 0, 1 and 3 are done; Task 2 is still open and the
measurements below have been redone on this machine. Task 4 has lost its first
three entries. Each task now says so at the top rather than reading as though
nothing has happened.

Read this whole file before touching anything. Run the tasks in order.

Repo root: D:\fpt      Project: D:\fpt\pharmacy-search      Branch: main


--------------------------------------------------------------------------------
STATE OF THE TREE - READ THIS FIRST
--------------------------------------------------------------------------------
Everything described in this file is COMMITTED. (It was not when the file was
written; the warning that used to be here no longer applies.) The work landed
across a9df28c and the three commits before it, and the three commits on
branch `task4-backups-and-hardening`:

    c32bcf1  Litestream WAL replication + rehearsed restore     [F-13]
    92bf470  Reviews restricted to buyers                       [N-08]
    e5967c7  Structured JSON logs to stdout

That branch has NOT been merged or pushed. Fast-forward main onto it if you
want it, or keep working on it.

Last verified full test run: 192 passed, 1 xfailed.
(157 + 1 when this file was written; 121 + 1 before that session.)
The frontend builds clean via `npm run build`.

NOT verified: `docker build`. Docker was not running on the machine that made
the last three commits, and the Dockerfile changed (it now downloads and runs
litestream). CI's `image` job builds it on push, so the first push is the test.


--------------------------------------------------------------------------------
TASK 0 - ORIENT (do this first, ~2 minutes)      [DONE - the tree is clean now]
--------------------------------------------------------------------------------
The file list below is historical: all of it is committed, so `git status`
comes back empty and `git diff --stat` prints nothing. Kept as a record of
what the 2026-09-11 session produced.

Run these and read the output before editing anything:

    cd /d/fpt
    git status --short
    git diff --stat

Expect ~32 modified files, plus these new untracked files:

    LICENSE
    NEXT-STEPS.txt                                    (this file)
    pharmacy-search/README.md
    pharmacy-search/.env.example
    pharmacy-search/.github/workflows/ci.yml
    pharmacy-search/backend/app/config.py
    pharmacy-search/backend/app/ratelimit.py
    pharmacy-search/backend/requirements-dev.txt
    pharmacy-search/backend/tests/test_prescriptions.py
    pharmacy-search/backend/tests/test_concurrency.py
    pharmacy-search/backend/tests/test_ratelimit.py
    pharmacy-search/backend/tests/test_search_catalog.py
    pharmacy-search/backend/tests/test_session_security.py


--------------------------------------------------------------------------------
TASK 1 - VALIDATE THE CORRECTOR CHANGE                                   [DONE]
--------------------------------------------------------------------------------
RESULT: validated, and the harness that was supposed to validate it turned out
to be the thing that needed fixing first. See a9df28c.

At n=500 the held-out harness has a 95% interval of about +/-0.022 around a
top-1 of 0.93, so the "top1 >= 0.930, identical to baseline" gate below fails
on sampling noise alone - four runs of IDENTICAL code landed at 0.922, 0.926,
0.930 and 0.934. `eval/held_out.py --full` now scores all 5,955 cases instead,
narrowing the interval to about +/-0.007, and the result carries top1_ci95 and
a `sampled` flag so two runs cannot be compared without their error bars.

On the full population the scaled bound holds at top-1 0.912 across four hash
seeds. That is not a regression from 0.930 - it is the same corrector measured
without the sampling wobble.

This also retires the claim below that the unscaled bound was *measured* to
lose accuracy (0.930 -> 0.926). That delta was a fifth of the instrument's
error at the time. The scaled bound is still the correct one, on the argument
in char_signature's docstring rather than on that measurement, and a direct
check found no violations over 2,029,059 vocabulary pairs. Everything from
here to the end of Task 1 is kept for the reasoning, not the numbers.

WHAT WAS DONE
app/corrector.py `_fuzzy_lookup` was scanning every vocabulary form in the +/-2
length buckets across two views, running an O(n*m) edit-distance DP on each.
That is audit finding F-08 (search p95 far over its 50ms budget).

A character-signature prune was added. `char_signature()` builds a bitmask of
which characters a string contains, and the scan now skips any form whose
signature differs from the query's by more character classes than the distance
cap could possibly cover - two integer operations instead of a DP table.

THE SUBTLE PART - DO NOT "SIMPLIFY" THIS
The bound is scaled by SUB_COST_ADJACENT (0.5):

    edit_distance(a,b) >= popcount(sig(a) & ~sig(b)) * SUB_COST_ADJACENT

Dropping that factor looks like a tighter, cleaner bound. It is simply false.
Keyboard-adjacent substitutions cost 0.5 each, so two of them change two
character classes for a total distance of only 1.0. I made exactly this mistake
mid-session and measured the damage: held-out top-1 fell from 0.930 to 0.926 -
it silently loses the very typos this corrector exists to catch. The scaled
(correct) version is what is in the tree now. Leave the factor alone.

MEASUREMENTS SO FAR (all on this machine; the PDF's numbers came from another
machine, so compare against these, not against the PDF)

    original code           p95 1349 ms   (999-query harness)
    unscaled/wrong bound    p95 1.55 ms   but top-1 0.926  <- LOST ACCURACY
    scaled/correct bound    ~128 ms for "etrogen" (single-query spot check)
                            ACCURACY NOT YET MEASURED      <- this is the gap

    baseline accuracy to match:
        top1_accuracy 0.930   top3_recall 0.938   no_candidates_rate 0.040

WHAT YOU MUST DO

1a. Run the held-out harness and compare against that baseline. It takes
    several minutes - run it in the background and wait for the completion
    notification rather than polling it.

        cd /d/fpt/pharmacy-search/backend
        python eval/held_out.py

    PASS CRITERIA: top1_accuracy >= 0.930 AND no_candidates_rate <= 0.040,
    i.e. identical to baseline. The prune is meant to be an exact lower bound,
    so ANY accuracy change means the bound is wrong. Debug it; do not accept it.
    Fastest way to isolate a regression: temporarily set
    `max_class_diff = 999` (which disables the prune) and confirm the numbers
    return to baseline.

1b. Run the full latency harness for a real p95:

        python eval/latency.py

    Record the number. Expect roughly 100-150 ms p95 - about 10x better than
    the 1349 ms baseline, but still over the 50 ms budget.

1c. Run the full suite:

        python -m pytest tests -q

    Expect 157 passed, 1 xfailed. The xfail is
    tests/test_core.py::test_correction_latency_budget, marked strict=True.
    It still xfails with the current prune (confirmed: "etrogen" is ~128 ms,
    over the 50 ms budget), so the suite stays green.

    IMPORTANT: if you later make the corrector fast enough, that test will
    XPASS, and strict=True turns an unexpected pass into a FAILURE. That is
    deliberate - it is how the suite tells you the gap closed. At that point,
    convert it from an xfail into a normal passing assertion and say so.


--------------------------------------------------------------------------------
TASK 2 - FINISH F-08 PROPERLY               (STILL OPEN - start here, measured)
--------------------------------------------------------------------------------
MEASURED 2026-09-18, on this machine, before any of the work below:

    eval/latency.py     p50 0.048 ms   p95 71.252 ms   p99 93.789 ms
                        max 208.257 ms      (budget: 50 ms)

Profiling the fuzzy path over the same 999-query mix, per query:

    2,937 bucket entries scanned (two integer ops each)  -> a few ms
      578 edit_distance DP calls surviving the prune     -> the rest

So the prune is NOT the bottleneck; the DP calls it lets through are. That
changes the recommendation below.

READ THIS BEFORE BUILDING A DELETION INDEX. SymSpell assumes unit-cost edits.
Here a keyboard-adjacent substitution costs SUB_COST_ADJACENT (0.5), so a
weighted cap of 3.0 admits up to SIX edit operations, and a deletion index
would need depth 6 rather than depth 2 to be lossless. Depth 6 over ~24k forms
is not a memory trade worth making, and depth 2 silently drops exactly the
multi-adjacent-key typos the 0.5 cost exists to catch. The deletion index is
the wrong tool for THIS distance metric.

Three cheaper changes were prototyped and measured instead (scratch only, not
committed - they need re-running end to end and a --full held-out before they
go in):

  1. SHARPER PRUNE, still an exact lower bound. A character class present in
     the query and absent from the form costs 0.5 only if some KEYBOARD
     NEIGHBOUR of it appears in the form; otherwise the cheapest operation
     touching it is a delete or a non-adjacent substitution, at 1.0. So

         bound = 0.5 * popcount(miss & nbr_sig(form))
               + 1.0 * popcount(miss & ~nbr_sig(form))

     with nbr_sig precomputed per form at startup. Four integer ops instead of
     two, and it can only ever RAISE the bound. Collisions in the `ord & 63`
     packing shrink it, which is the safe direction - they can never break it.

  2. MERGE THE VIEWS. The telex and no-accent indexes share 6,277 forms, and
     _fuzzy_lookup currently runs the DP on each of them twice. Merging to one
     form -> keywords map is behaviour-identical (the loop already takes the
     min distance per keyword) and drops the scan from 29,937 entries to
     23,660.

  3. BANDED ROLLING DP. edit_distance allocates a full (la+1)x(lb+1) list of
     lists and calls is_keyboard_adjacent per cell. A single-row version with
     a +/-3 Ukkonen band and the neighbour set hoisted per row was verified
     identical to the current function on 180,000 random pairs across all
     three caps. (KEYBOARD_NEIGHBORS is symmetric - checked - so one lookup
     per cell suffices instead of two.)

Whatever you do, re-run `eval/held_out.py --full` and treat the accuracy as
the product. And note the 50 ms budget only needs a ~1.5x improvement now,
not the 10x the original 1349 ms figure implied.

The original suggestion, kept for the record:

Recommended: SymSpell-style deletion neighbourhood.
  - In Corrector.__init__, for each form in the telex and no-accent views,
    generate every string obtainable by deleting up to 2 characters, and map
    each to the forms that produced it.
  - At query time, do the same for the token and look up the union.
  - Run the real edit_distance only on that shortlist.

RISK AND HOW TO HANDLE IT: MAX_EDIT_DIST is 3, so a depth-2 deletion index can
miss distance-3 candidates and WILL cost recall. Measure it. If top-1 drops
below 0.930, either raise the deletion depth (memory cost) or keep the
signature prune as a fallback full scan for tokens that find nothing.

Whatever you try, re-run eval/held_out.py every single time and treat 0.930
top-1 as a hard floor. Accuracy is the product here; latency is second.

Cheaper and also worth doing: an LRU cache on Corrector.correct(). The index is
immutable at runtime, so there is nothing to invalidate. It will not move the
benchmark (999 distinct queries) but real traffic repeats heavily.


--------------------------------------------------------------------------------
TASK 3 - COMMIT THE WORK                                                 [DONE]
--------------------------------------------------------------------------------
Everything is committed. The nine-way split below did NOT happen: the work
landed as a9df28c plus three commits with messages "a" and "update". That
history is already published, so it was left alone rather than rebased.

The split is still the right instinct, and the three commits on
`task4-backups-and-hardening` follow it - one finding per commit, with the
measurements in the message.

Suggested split, in this order (historical):

  1. "Add env-driven config and move mutable state onto the volume"
     app/config.py, db.py paths, learning/*, eval/query_log.py,
     build/aggregate_clicks.py, build/build_nearmiss.py, admin_reports.py,
     Dockerfile, fly.toml, .gitignore, .env.example            [F-02, F-12]

  2. "Stop returning the password reset token; invalidate sessions"
     app/auth.py, tests/test_auth.py, tests/test_session_security.py
                                                               [F-01, N-06]

  3. "Add rate limiting and input caps to unauthenticated endpoints"
     app/ratelimit.py, main.py, tests/test_ratelimit.py, conftest.py
                                                               [F-05, N-03]

  4. "Hold prescription-only orders for pharmacist review"
     db.py schema, checkout.py, cart.py, admin_orders.py, frontend views,
     tests/test_prescriptions.py                                     [F-03]

  5. "Make the stock decrement the stock check"
     checkout.py, db.py write_transaction, admin_orders.py,
     tests/test_concurrency.py                                 [F-06, N-02]

  6. "Answer search from the product table, not a snapshot"
     catalog.py search_catalog, main.py, frontend search.js,
     tests/test_search_catalog.py                                    [N-01]

  7. "Add indexes, numbered migrations and an admin audit log"
     db.py, inventory.py, admin_users.py, admin_corrections.py
                                                         [F-07, F-09, N-09]

  8. "Add security headers, CI, README, licence and pinned dependencies"
     main.py middleware, .github/workflows/ci.yml, README.md, LICENSE,
     requirements.txt, requirements-dev.txt                    [F-10, N-05]

  9. The corrector prune - only once Task 1 validates it. Put the measured
     before/after numbers in the commit message.                     [F-08]

Commit message trailers for this repo:

    Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
    Claude-Session: https://claude.ai/code/session_01DMF5bCmfCN6gyuCjLdLiLJ


--------------------------------------------------------------------------------
TASK 4 - WHAT IS STILL NOT DONE
--------------------------------------------------------------------------------
In the order I would do them.

F-13  BACKUPS.                                            [DONE - c32bcf1]
      Litestream now runs as the container's supervisor, streaming the WAL to
      S3-compatible storage every second (12h snapshots, 30 day retention).
      docker-entrypoint.sh restores BEFORE uvicorn starts, because main.py
      creates an empty database at import and would otherwise replicate that
      emptiness over the good replica. `-if-replica-exists` is reachable only
      behind LITESTREAM_BOOTSTRAP for the same reason: a misconfigured bucket
      must crash-loop, not boot empty.

      scripts/restore-drill.sh rehearses recovery. `local` runs in CI on every
      push against a file:// replica and fails if a row written after the last
      snapshot does not come back. `remote` restores the production replica
      into a scratch file and verifies it.

      STILL TO DO: nobody has run `restore-drill.sh remote` against a real
      bucket, because that needs credentials this machine does not have. The
      recovery path is tested, not proven. Create the bucket, set the Fly
      secrets listed in .env.example, deploy once with LITESTREAM_BOOTSTRAP=1,
      UNSET IT, then run the remote drill.

F-04  SESSION TOKENS IN localStorage. Partly mitigated this session: TTL cut
      from 30 days to 7, sessions invalidated on password change and reset,
      expired rows purged, CSP added. Remaining work is moving to an
      httpOnly; Secure; SameSite=Lax cookie plus CSRF protection on
      state-changing requests.
      NOTE: the frontend escapes consistently across all 15 view files, so
      there is no known injection path today. This is defence in depth, not an
      open hole - do not describe it as one.

F-14  HASH-ROUTER SPA IS INVISIBLE TO SEARCH ENGINES. Move to History API paths
      with a server-side SPA fallback (main.py currently relies on the hash
      router and explains that in a comment - update the comment too), add
      per-product <title>/meta/Open Graph tags, Product JSON-LD, sitemap.xml.

F-15  UI IS ENGLISH, CATALOGUE IS VIETNAMESE. Extract strings into a locale
      file, ship vi-VN first, and format money as 145.000 d via
      Intl.NumberFormat instead of English separators. The users who need
      Telex-tolerant search are the ones reading Vietnamese.

F-11  IMAGES HOTLINK cdn.nhathuoclongchau.com.vn. Mirror to your own object
      storage behind a CDN and serve resized WebP/AVIF. The CSP in main.py
      currently allows that CDN in img-src - tighten it when you migrate.

F-07b FTS5. Indexes were added, but catalogue search still uses LIKE '%term%',
      which no B-tree index can serve. Move to SQLite FTS5 over search_text.

F-12b POSTGRES OR LITEFS. Still one machine. min_machines_running is now 1 and
      a healthcheck exists, but SQLite on a single volume still forecloses
      zero-downtime deploys and redundancy.

N-07  USER ENUMERATION via /api/auth/register returning 409 "An account with
      this email already exists". Deliberately left alone: it is a genuine
      tradeoff against registration UX, and rate limiting mitigates it.
      Revisit only alongside email verification.

N-08  REVIEWS ARE OPEN TO ANY SIGNED-IN ACCOUNT.          [DONE - 92bf470]
      Closed. catalog._has_purchased is now the single predicate behind both
      the gate and the verified_purchase stamp, so the two cannot drift. The
      six tests that reviewed without buying now buy first; three new ones
      cover the gate itself, the verified stamp, and the order-then-cancel
      workaround. The frontend explains the rule instead of offering a form
      that can only 403.

ALSO NOT DONE (from the audit's operations sections, all still valid):
  - Staging environment as a second Fly app (audit section 05, step 01).
  - Sentry for exceptions, plus an external uptime monitor.
  - Structured JSON logs to stdout for a log drain.       [DONE - e5967c7]
    One JSON object per line, one per request, carrying method, path, status,
    duration_ms and request_id; request_id reuses Fly's fly-request-id and
    comes back in X-Request-Id. 4xx warns, 5xx errors, /api/health is debug.
    Query strings are deliberately not logged - there is a test that says so.
    What is still missing is the drain itself: nothing is attached, so these
    lines currently live and die in `fly logs`.
  - Real payments (VNPay/Momo). checkout.py is honest that only COD works.
  - Transactional email. Password reset currently records a token that only an
    operator reading the database can retrieve. That is the correct failure
    mode, but it is NOT a working reset flow. Wire up Resend/Postmark/SES.
  - eval/human_typed.tsv is still just a header row. Sit ten people down, have
    them type 100 product names quickly, record it. Until that file has rows,
    92.6% is a lower-bound proxy, not user-facing accuracy.
  - Session-linked query logs, so correction precision (the harness's own
    metric #1) becomes computable. main.py already logs session_id; it needs
    an outcome field linking consecutive searches.


--------------------------------------------------------------------------------
GOTCHAS - THINGS THAT WILL BITE YOU
--------------------------------------------------------------------------------
* tests/conftest.py sets PHARMACY_RATE_LIMIT_ENABLED=0, because the suite
  registers dozens of accounts from one client and would otherwise trip its own
  limiter. tests/test_ratelimit.py re-enables it with a monkeypatch fixture. If
  you add a test that needs limiting on, copy that fixture pattern.

* conftest also redirects PHARMACY_DATA_DIR, PHARMACY_SEARCH_DB,
  PHARMACY_LOG_DIR and PHARMACY_LEARNING_DIR into a temp directory. Never point
  tests at backend/data.

* tests/test_checkout.py::_in_stock_product deliberately filters to
  NON-prescription products. If you "simplify" that filter away, every checkout
  test breaks, because Rx items now go to awaiting_prescription instead of
  placed. There is a _prescription_product helper right next to it.

* PHARMACY_EXPOSE_RESET_TOKEN puts the reset token back into the HTTP response.
  It defaults OFF and must NEVER be set in a deployed environment. Tests read
  the token from the database instead - see _reset_token_from_db in
  tests/test_auth.py.

* Schema changes go in db.py MIGRATIONS as a new numbered entry. Do NOT rely on
  adding a column to the CREATE TABLE in SCHEMA: that only affects databases
  created afterwards, which is exactly the drift the migration table exists to
  prevent. I made that mistake once this session (a UNIQUE constraint on
  reviews) and fixed it by moving it to a CREATE UNIQUE INDEX in INDEXES.

* SEARCH_TEXT_FOLD_VERSION in db.py must be bumped whenever search_key()
  changes how names are folded, or existing rows keep a stale search_text.

* Local state moved. backend/logs/ and backend/learning/*.json now live under
  backend/data/logs/ and backend/data/learning/. The existing files were moved
  there during the session, not deleted.

* eval/held_out.py and eval/latency.py construct Corrector() directly rather
  than through get_corrector(), so they are unaffected by the config path
  changes. That is also why they are a clean measurement of corrector changes.

* The eval harness is slow (several minutes). Run it in the background and wait
  for the completion notification instead of polling it.


--------------------------------------------------------------------------------
VERIFY EVERYTHING STILL WORKS
--------------------------------------------------------------------------------
    cd /d/fpt/pharmacy-search/backend
    python -m pytest tests -q            # expect 192 passed, 1 xfailed
    python eval/held_out.py --full       # expect top1 0.912 +/- 0.007
    python eval/latency.py               # last measured p95 71.252 ms

    cd /d/fpt/pharmacy-search/frontend
    npm run build                        # must succeed

    cd /d/fpt/pharmacy-search
    docker build -t pharmacy-search:local .

    cd /d/fpt/pharmacy-search
    ./scripts/restore-drill.sh local     # needs litestream + sqlite3 on PATH

The docker build has STILL NOT been verified locally - once because Docker was
not installed, and again on 2026-09-18 because Docker Desktop was not running.
It has changed twice since (npm ci, non-root user, split requirements,
HEALTHCHECK; then the litestream binary, /etc/litestream.yml and the
entrypoint). CI's `image` job builds it on every push, which is the only place
it has been exercised. Run it before deploying anything.


--------------------------------------------------------------------------------
ONE LINE TO PASTE TO CLAUDE NEXT TIME
--------------------------------------------------------------------------------
    Read NEXT-STEPS.txt in this folder, then do Task 2.
================================================================================
